# Variables: 
 -  barialbles , in python are referece to object defined with a value in it. 
 - every thing in pyhon is in an object
    ```
    x =  5 
     // for python this is means 
      - create a object of type integer and put 5 in int 
      - then point x in to that object address 
      - later if i change the value of 5 in will create another object and change the address pointed by the x to the new varible.

    ```
    ## Static and Dynamic typing

    ### Types:
     a type of varible is metadata that tells computer 
      -  how much memory should be allocated 
      -  what operations are allowed on it 
      -  how to inteprete it to bits in memory.

    ### Static Typing (eg. C++)
      -  is when a varible is associated with type and compiler enforce it at compile time.
       ```
        int x = 5; 
         // this mean in c++ 
          - create a varible with bit size of 32/64 bit .
          - only arthimetic (may be others also) are allowed
          - and put 5 in to int.

       ```  
    ### dynamic Typing (eg.python)
     - is when the varible is infered at run time . 
     - in python this is allowed b/c variables are refereces to an object and re assignment is just changing the reference.